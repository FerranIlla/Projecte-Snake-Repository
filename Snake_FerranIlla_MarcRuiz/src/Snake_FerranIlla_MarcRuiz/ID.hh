#pragma once
#include <iostream>

// Enum ID used to identify each Game Object in the Game
enum ObjectID {
	SNAKE_HEAD, 
	SNAKE_BODY, 
	WALL, 
	FOOD,
	MENU, 
	HEART
};

enum  FontID {
	ARIAL
	//ADD MORE FONTS IF NEEDED
};

